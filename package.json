{
  "name": "payroll-mail-service",
  "version": "1.0.0",
  "description": "Reliable bulk email service: send a personalized email with one attachment to many recipients in scheduled batches via Gmail/Workspace SMTP. Server-side background sending survives closing the browser.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "adm-zip": "^0.5.17",
    "better-sqlite3": "^11.8.1",
    "csv-parse": "^5.6.0",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^8.0.10",
    "xlsx": "^0.18.5"
  }
}
